{% extends "Partie_admin/Shared/base.html.twig" %}

{% block title %}
    Plans
{% endblock %}

{% block body %}
    <h1>
        Plans
    </h1>

    <div class="ml-auto text-right">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                <div class="row">
                     <div class="col-md-3">
                        <a class="btn btn-primary" href="{{ path('miniplans_index') }}">
                        Liste des plans
                        </a>
                         &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
                    </div>   



                    {% if app.user.id ==  miniplan.user.id %}
                <div class="col-md-3">

                    <a class="btn btn-success" href="{{ path('miniplans_edit', {'id': miniplan.id}) }}">
                        Modifier
                    </a>
                     &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
                </div>   

                
                <div class="col-md-3">

                    <form method="post" action="{{ path('miniplans_delete', {'id': miniplan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ miniplan.id) }}">
                                                        <input type="hidden" name="_method" value="DELETE">
                                                            <button class="btn btn-danger " style="color:#fff;">
                                                                Supprimer
                                                            </button>
                    </form>   
                   
                </div>   
                </div>   


                {% endif %}

                    
                    {# {% if  plan.achats  %}
                                {% for fichier in plan.fichiers %}
                            <a class="btn btn-success" href="{{ asset('/pdf/' ~ fichier.nom) }}">Telecharger le Plan</a>
                            {% endfor %}
                            {% endif %} #} </li> </ol> </nav> </div> <table class="table"> 
                            <tbody> 
                                <tr>
                                    <th>id<th>
                        <td>
                            {{ miniplan.id }}
                        </td>
                    </tr>
                    <tr>
                        <th>
                            Titre
                        </th>
                        <td>
                            {{ miniplan.titre }}
                        </td>
                    </tr>


                    <tr>
                        <th>
                            Maison
                        </th>
                        <td>
                            {{ miniplan.plans }}
                        </td>
                    </tr>
                </tbody>
            </table>

        </div>
    </div>
</div></div>{% endblock %}